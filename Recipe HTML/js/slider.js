$(document).ready(function(){
  $('.hero-slider').slick({
    infinite: true,
    autoplay: true,
    autoplaySpeed: 3000,
    arrows: true,
    fade: true,
    speed: 800,
    cssEase: 'linear'
  });
});
